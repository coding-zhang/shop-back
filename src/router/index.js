import Vue from 'vue'
import VueRouter from 'vue-router'
Vue.use(VueRouter)


// 路由表
const routes = [
  // {
    
  // }
]

// 路由器实例
const router = new VueRouter({
  routes
})

// 导出路由器
export default router